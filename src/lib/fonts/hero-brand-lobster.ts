import { Black_Ops_One } from "next/font/google";

/** Black Ops One — лише для hero-логотипу ZENTO, не для всього сайту. */
export const heroBrandLobster = Black_Ops_One({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
